package com.bedu.pokedex;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class ColoresActivity extends AppCompatActivity {

    /*
    EN este ejemplo vamos a crear un frame layout y dos layouts dentro, para ver como funciona
    el eje Z en las apps de Android. revisa el XML
     */
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_colores);
    }
}
